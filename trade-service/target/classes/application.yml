server:
  port: 8082

spring:
  application:
    name: trade-service
  datasource:
    url: jdbc:mysql://localhost:3306/db_trade?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: L050325lgh@
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848